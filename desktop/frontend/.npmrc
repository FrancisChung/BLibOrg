; Node's experimental global localStorage (default-on in recent Node
; versions) shadows jsdom's implementation and lacks .clear(), breaking
; localStorage-touching tests. Disable it here.
node-options=--no-experimental-webstorage
